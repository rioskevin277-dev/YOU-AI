/**
 * SectionBlend — Pure CSS gradient divider between sections.
 * 160px height, -80px margin-top, two half-gradients blending #050505 ↔ transparent.
 * No JS, no props.
 */
export function SectionBlend() {
  return <div className="section-blend" aria-hidden="true" />;
}
